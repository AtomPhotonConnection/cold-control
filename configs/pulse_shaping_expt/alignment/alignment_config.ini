date = 11/03/2026
time = 14:00
notes = "Alignment config for MOT fluorescence. Runs the same experiment repeatedly until the user stops it."

save location = "C:\pulse_shaping_data"
mot reload = 1000
iterations = 7

sequence_config = "configs\pulse_shaping_expt\sequence\readout_with_MOTC.ini"
scope_config = "configs\pulse_shaping_expt\scope\keysight_feb26.ini"
awg_config = "configs\pulse_shaping_expt\awg_configs\feb26_awg_updated.ini"

# Optional: path to a folder containing background measurement data.
# If provided, the alignment will display F_norm (normalised fluorescence).
# If omitted, it falls back to displaying raw F_img.

[metadata]
config_type = experiment
experiment_type = "MOT Fluorescence Alignment"
